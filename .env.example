# DEEPSEEK_API_KEY: 唯一的语言引擎密钥。
# 方式 A（推荐，无需 .env）：在游戏首页的 [API 配置] 面板中填写，密钥只存在浏览器 localStorage。
# 方式 B：复制本文件为 .env 并填入，仅在使用 npm run dev / npm start 时由服务端读取。
DEEPSEEK_API_KEY=""

# APP_URL: 应用对外可访问的地址，用于自引用链接与回调。
APP_URL="MY_APP_URL"
